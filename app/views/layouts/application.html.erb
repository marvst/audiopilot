<!DOCTYPE html>
<html data-theme="dark">
  <head>
    <title>Audio Pilot</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css" />
    <%# <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-v4-grid-only@1.0.0/dist/bootstrap-grid.min.css" /> %>
  </head>

  <body>
    <nav class="container">
      <ul>
        <li>
          <a href="/"><strong>Audiopilot</strong></a>
        </li>
      </ul>
      <% if current_user %>
      <ul>
        <li>
          <a href="/signout">Sign out</a>
        </li>
      </ul>
      <% end %>
    </nav>
    <main class="container">
      <% if flash[:info] %>
        <article style="background-color:rgb(27, 78, 94); color: #fff">
            <%= flash[:info] %>
        </article>
      <% elsif flash[:success] %>
        <article style="background-color: #1b5e20; color: #fff">
            <%= flash[:success] %>
        </article>
      <% elsif flash[:error] %>
        <article style="background-color:rgb(94, 28, 27); color: #fff">
            <%= flash[:error] %>
        </article>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
